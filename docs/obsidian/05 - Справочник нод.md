---
tags:
  - testbuilder
  - nodes
  - reference
updated: 2026-07-16
---

# Справочник нод

Эта страница описывает все ноды, существующие в проекте на 2026-06-30: 28 видимых нод палитры, 2 технические ноды вложенного графа `For Slaves` и их runtime-шаги.

## Как читать справочник

Для каждой ноды указаны:

- `UI title` - заголовок в приложении.
- `JSON type` - значение поля `type` при сохранении.
- `ViewModel` - визуальная модель ноды.
- `Step` - runtime-логика, которую создает `GraphCompiler`.
- `Входы/выходы` - коннекторы и переходы.
- `Параметры` - редактируемые поля и дефолты.
- `Результат` - какие `StepResult` возвращает шаг.
- `Контекст` - какие переменные пишет или читает `TestContext`.
- `Особенности` - важные ограничения.

## Общие правила переходов

| Результат шага | Что делает `TestExecutor` |
|---|---|
| `Next` | Берет `TestNode.Next`. |
| `True` | Берет `TestNode.OnTrue`. |
| `False` | Берет `TestNode.OnFalse`; если его нет, граф завершается как `Failed`. |
| `Stop` | Завершает текущий граф как `Completed`. |

Большинство условных нод имеют `True` и `False`. Линейные ноды имеют один выход и возвращают `Next`.

## Индекс нод

| Категория палитры | Ноды |
|---|---|
| `Структура` | [[#Старт]], [[#Конец]], [[#Метка]], [[#Задержка]], [[#Подтест]], [[#Цикл For]] |
| `Modbus` | [[#Запись регистра]], [[#Проверка диапазона]], [[#Проверка равенства]], [[#Ожидание значения]], [[#Опрос регистра]] |
| `Проверки` | [[#Selftest Check]], [[#Check Variable Equality]], [[#Check Variable Range]] |
| `HTTP и сеть` | [[#Clear ARP Cache]], [[#Get Serial Number]], [[#Send UDP Set MAC]], [[#Run Data Test]], [[#Get UPS Status]], [[#Get UPS Voltage]], [[#Get IRP Status]], [[#Wait Variable Until]] |
| `Оператор и отчеты` | [[#Действие оператора]], [[#Build MAC From Serial]], [[#Compare Variables]], [[#Build Test Report]], [[#Print Label]], [[#Send Test Report]] |
| Технические ноды | [[#Тело начало]], [[#Тело конец]] |

## Старт

| Поле | Значение |
|---|---|
| UI title | `Старт` |
| JSON type | `Start` |
| ViewModel | `StartNodeViewModel` |
| Step | `StartStep` |
| Палитра | `Структура` |

**Коннекторы:** выход `Выход`.

**Параметры:** нет.

**Поведение:** пишет в лог `[ШАГ] Тест начат.` и возвращает `Next`.

**Особенности:** `GraphCompiler` ищет `StartNodeViewModel`, если в графе нет `BodyStartNodeViewModel`. В обычном корневом графе эта нода обязательна.

## Конец

| Поле | Значение |
|---|---|
| UI title | `Конец` |
| JSON type | `End` |
| ViewModel | `EndNodeViewModel` |
| Step | `EndStep` |
| Палитра | `Структура` |

**Коннекторы:** вход `Вход`.

**Параметры:** нет.

**Поведение:** пишет `[OK] Тест завершён.` и возвращает `Next`. Если после нее нет связи, executor доходит до `current = null` и завершает граф как `Completed`.

## Тело начало

| Поле | Значение |
|---|---|
| UI title | `Тело: начало` |
| JSON type | `Body Start` |
| ViewModel | `BodyStartNodeViewModel` |
| Step | `PassThroughStep` |
| Палитра | Техническая, создается внутри `For Slaves` |

**Коннекторы:** выход `Выход`.

**Параметры:** нет.

**Поведение:** возвращает `Next`. Используется как стартовая нода body graph, потому что `GraphCompiler` предпочитает `BodyStartNodeViewModel` перед обычным `Start`.

**Особенности:** защищена от удаления в графах, где `UsesBodyBoundaryNodes = true`. Поддерживает `Clone()` для копирования составных нод с вложенным body graph.

## Тело конец

| Поле | Значение |
|---|---|
| UI title | `Тело: конец` |
| JSON type | `Body End` |
| ViewModel | `BodyEndNodeViewModel` |
| Step | `BodyEndStep` |
| Палитра | Техническая, создается внутри `For Slaves` |

**Коннекторы:** вход `Вход`.

**Параметры:** нет.

**Поведение:** пишет успешное завершение итерации текущего `CurrentSlaveId` и возвращает `Stop`.

**Зачем нужен `Stop`:** он завершает только текущий вложенный граф. `ForEachSlaveStep` после этого продолжает следующую итерацию.

**Особенности:** защищена от удаления в графах, где `UsesBodyBoundaryNodes = true`. Поддерживает `Clone()` для копирования составных нод с вложенным body graph.

## Метка

| Поле | Значение |
|---|---|
| UI title | `Метка` |
| JSON type | `Label` |
| ViewModel | `LabelNodeViewModel` |
| Step | `LabelStep` |
| Палитра | `Структура` |

**Коннекторы:** вход `Вход`, выход `Выход`.

**Параметры:**

| Параметр | Дефолт | Назначение |
|---|---|---|
| `Text` | `Этап` | Текст заметки на графе. |
| `LabelWidth` | `300` | Ширина визуальной метки. |
| `LabelHeight` | `120` | Высота визуальной метки. |

**Поведение:** пишет текст метки в лог и возвращает `Next`.

**Особенности:** нода полезна для документации сценария прямо внутри графа. Она не меняет состояние стенда.

## Задержка

| Поле | Значение |
|---|---|
| UI title | `Задержка` |
| JSON type | `Delay` |
| ViewModel | `DelayNodeViewModel` |
| Step | `DelayStep` |
| Палитра | `Структура` |

**Коннекторы:** вход `Вход`, выход `Выход`.

**Параметры:**

| Параметр | Дефолт | Назначение |
|---|---|---|
| `Milliseconds` | `1000` | Длительность паузы. |

**Поведение:** логирует начало задержки, ожидает `Task.Delay(Milliseconds, cancellationToken)`, логирует завершение и возвращает `Next`.

**Особенности:** корректно прерывается при остановке теста.

## Подтест

| Поле | Значение |
|---|---|
| UI title | значение `Name`, по умолчанию `Подтест` |
| JSON type | `Subtest` |
| ViewModel | `SubtestNodeViewModel` |
| Step | `SubtestStep` |
| Палитра | `Структура` |

**Коннекторы:** вход `In`, выходы `Success`, `Error`.

**Параметры:**

| Параметр | Дефолт | Назначение |
|---|---|---|
| `Name` | `Подтест` | Название ноды и вложенного графа. |
| `Description` | пусто | Описание подтеста. |
| `IsEnabled` | `true` | Если `false`, тело не выполняется. |
| `StopOnError` | `true` | Если тело провалилось, бросать исключение вместо обычного `False`. |
| `RunOnFailure` | `false` | Запускать тело как аварийный cleanup после провала, остановки или исключения основного графа. |

**Вложенный граф:** `BodyGraph` с обычными `Start` и `End`. `BodyGraph.UsesBodyBoundaryNodes = false`.

**Поведение:**

- выключенный подтест возвращает `True`;
- успешное тело возвращает `True`;
- failed тело при `StopOnError = false` возвращает `False`;
- failed тело при `StopOnError = true` бросает `InvalidOperationException`.

**Контекст:** использует тот же `TestContext`, что и родительский граф. Переменные и `RegisterState` общие.

**UI-прогресс:** во время выполнения `SubtestNodeViewModel` показывает прогресс вида `N/M: Title`.

**Cleanup:** `RunOnFailure` не запускает подтест сам по себе во время обычного прохода
графа. После неуспешного завершения основного запуска `RunGraphAsync` отдельно
выполняет все включенные `Subtest` с этим флагом и дает им общий timeout 60 секунд.

## Цикл For

| Поле | Значение |
|---|---|
| UI title | `Цикл For` |
| JSON type | `For Slaves` |
| ViewModel | `ForEachSlaveNodeViewModel` |
| Step | `ForEachSlaveStep` |
| Палитра | `Структура` |

**Коннекторы:** вход `In`, выходы `True`, `False`.

**Параметры:**

| Параметр | Дефолт | Назначение |
|---|---|---|
| `FromSlaveId` | `1` | Первый slave ID. |
| `ToSlaveId` | `20` | Последний slave ID. |
| `Step` | `1` | Шаг инкремента. Если `0`, runtime заменяет на `1`. |
| `StopOnError` | `true` | Остановить цикл при провале вложенного графа. |

**Вложенный граф:** `BodyGraph` с `Body Start` и `Body End`. `UsesBodyBoundaryNodes = true`.

**Поведение:**

1. Проверяет, что `FromSlaveId <= ToSlaveId`.
2. Для каждого slave задает `context.CurrentSlaveId`.
3. Пишет переменную `slaveId`.
4. Запускает вложенный граф.
5. При ошибке и `StopOnError = true` возвращает `False`.
6. При завершении всех итераций возвращает `Next`.

**Переходы:** `GraphCompiler` мапит успешный выход `True` не в `OnTrue`, а в `Next`. Ошибочный `False` идет в `OnFalse`.

**Особенности:** нода предназначена именно для диапазона Modbus slave ID. Для произвольного списка или цикла по условию нужна отдельная нода.

## Запись регистра

| Поле | Значение |
|---|---|
| UI title | `Запись регистра` |
| JSON type | `Write Register` |
| ViewModel | `ModbusWriteNodeViewModel` |
| Step | `ModbusWriteStep` |
| Палитра | `Modbus` |

**Коннекторы:** вход `Вход`, выходы `True`, `False`.

**Параметры:**

| Параметр | Дефолт | Назначение |
|---|---|---|
| `SlaveId` | `0` | Фиксированный slave ID. |
| `UseCurrentSlaveId` | `false` | Использовать `context.CurrentSlaveId` из `For Slaves`. |
| `Address` | `0` | Holding register address. |
| `Value` | `0` | Значение `ushort`. |
| `VerifyWrite` | `false` | После записи читать регистр и проверить значение. |

**Поведение:**

- пишет один регистр через `IModbusService.WriteRegisterAsync(..., verify: false)`;
- если `VerifyWrite = false`, при успешной записи возвращает `True`;
- если `VerifyWrite = true`, до трех раз читает регистр с паузой 300 мс и сравнивает с ожидаемым значением;
- при подтверждении обновляет `RegisterState`;
- при ошибке возвращает `False`.

**Особенности:**

- `UseCurrentSlaveId = true` требует выполнения внутри `For Slaves`.
- UI умеет показывать ComboBox slave/register, когда стенд подключен и `SlaveRegistry` заполнен.

## Проверка диапазона

| Поле | Значение |
|---|---|
| UI title | `Проверка диапазона` |
| JSON type | `Check Register Range` |
| ViewModel | `CheckRegisterRangeNodeViewModel` |
| Step | `CheckRegisterRangeStep` |
| Палитра | `Modbus` |

**Коннекторы:** вход `Вход`, выходы `True`, `False`.

**Параметры:** `SlaveId`, `UseCurrentSlaveId`, `Address`, `Min`, `Max`, `LiveRead`.

**Поведение:** при `LiveRead = false` берет последнее значение регистра из
`context.RegisterState`. При `LiveRead = true` перед проверкой читает holding
register через `IModbusService`, обновляет `RegisterState`, затем проверяет
`Min <= value <= Max`. Возвращает `True` или `False`.

**Особенности:** live-чтение требует активного Modbus-подключения и валидного адреса.
Без `LiveRead` значение должно быть заранее обновлено мониторингом или
`ModbusWriteStep` с verification.

## Проверка равенства

| Поле | Значение |
|---|---|
| UI title | `Проверка равенства` |
| JSON type | `Check Register Equality` |
| ViewModel | `CheckRegisterEqualityNodeViewModel` |
| Step | `CheckRegisterEqualityStep` |
| Палитра | `Modbus` |

**Коннекторы:** вход `Вход`, выходы `True`, `False`.

**Параметры:** `SlaveId`, `UseCurrentSlaveId`, `Address`, `ExpectedValue`, `LiveRead`.

**Поведение:** при `LiveRead = false` берет значение из `RegisterState`. При
`LiveRead = true` сначала читает регистр через `IModbusService`, обновляет
`RegisterState`, затем сравнивает с `ExpectedValue`. Возвращает `True` или `False`.

**Особенности:** по умолчанию работает по последнему известному значению. Прямое
чтение включается чекбоксом `Live Modbus read`.

## Ожидание значения

| Поле | Значение |
|---|---|
| UI title | `Ожидание значения` |
| JSON type | `Wait Until` |
| ViewModel | `WaitUntilNodeViewModel` |
| Step | `WaitUntilStep` |
| Палитра | `Modbus` |

**Коннекторы:** вход `Вход`, выходы `True`, `False`.

**Параметры:** `SlaveId`, `UseCurrentSlaveId`, `Address`, `ExpectedValue`, `TimeoutMs`, `LiveRead`.

**Поведение:** до истечения timeout каждые 200 мс проверяет значение. При
`LiveRead = false` источник - `RegisterState`; при `LiveRead = true` каждый poll
делает прямое чтение Modbus и обновляет `RegisterState`. Если найдено ожидаемое
значение - `True`, иначе по таймауту `False`.

**Особенности:** без `LiveRead` ожидание работает только если `RegisterMonitor`
параллельно обновляет нужный регистр.

## Опрос регистра

| Поле | Значение |
|---|---|
| UI title | `Опрос регистра` |
| JSON type | `Poll Register` |
| ViewModel | `PollRegisterNodeViewModel` |
| Step | `PollRegisterStep` |
| Палитра | `Modbus` |

**Коннекторы:** вход `Вход`, выходы `True`, `False`.

**Параметры:** `SlaveId`, `UseCurrentSlaveId`, `Address`, `Min`, `Max`, `SampleCount`, `LiveRead`.

**Поведение:**

- делает `SampleCount` замеров с интервалом 1000 мс;
- каждый замер берет значение из `RegisterState` или, при `LiveRead = true`, читает
  регистр напрямую через `IModbusService`;
- live-замер обновляет `RegisterState`;
- считает успехи и провалы;
- возвращает `True`, если успехов больше, чем провалов.

**Особенности:** без `LiveRead` нода опрашивает состояние, обновленное мониторингом.

## Selftest Check

| Поле | Значение |
|---|---|
| UI title | `Selftest Check` |
| JSON type | `Selftest Check` |
| ViewModel | `SelfTestCheckNodeViewModel` |
| Step | `SelfTestCheckStep` |
| Палитра | `Проверки` |

**Коннекторы:** вход `In`, выходы `True`, `False`.

**Параметры:**

| Параметр | Дефолт |
|---|---|
| `Url` | `http://192.168.0.1/cgi-bin/luci/admin/statistics/deviceinfo?luci_username=admin&luci_password=admin` |
| `TimeoutMs` | `160000` |
| `PollIntervalMs` | `5000` |
| `OutputPrefix` | `Dut` |
| `ValidationRules` | `init_ok=1..1`, `dev_type=0..65535`, `firmvare_vers=0..65535`, `boot_vers=0..65535` |
| `FailOnError` | `true` |

**Поведение:**

1. Для IP-адреса DUT фиксирует выбранный ОС исходный адрес маршрута. Для private/local
   назначения отключает системный proxy и в `HttpClient`, и в headless Chrome/Edge.
2. Пытается получить LuCI/deviceinfo через headless Chrome/Edge, если браузер найден.
3. Если браузер не найден или headless-запуск завершился ошибкой, пробует обычный HTTP GET; браузерный timeout ограничен так, чтобы оставался запас под HTTP fallback.
4. Для LuCI/deviceinfo URL дополнительно пробует legacy `http://host/test.shtml`, если основной ответ не содержит XML; `test.shtml` запрашивается обычным HTTP без headless browser.
5. До истечения `TimeoutMs` повторяет циклы запросов через `PollIntervalMs`, пока в ответе/DOM не появится валидный фрагмент `<selftest>...</selftest>` или legacy `<settings>...</settings>` с `default_mac`.
6. Сохраняет raw XML в `SelfTestRaw` внутри `TestContext` текущего запуска.
7. Парсит XML.
8. Все leaf-поля пишет в `context.Variables` с префиксом `OutputPrefix`.
9. Добавляет legacy-алиасы для `link_N`, `poe_*`, `sfp_*`.
10. Проверяет `ValidationRules` и логирует каждое правило отдельной строкой: факт,
   ожидаемый диапазон, `OK` или причина провала.
11. Возвращает `True` при успехе, `False` при ошибке.

**Контекст:**

- `SelfTest.Url`
- `SelfTest.StatusCode`
- `SelfTest.ElapsedMs`
- `SelfTest.Attempts`
- `SelfTest.PollIntervalMs`
- `SelfTest.DirectConnection`
- `SelfTest.RouteSourceAddress`
- `SelfTest.RouteDestinationAddress`
- `SelfTest.RouteError`
- `SelfTestRaw`
- `SelfTest.ParsedFieldCount`
- `SelfTest.CheckedRuleCount`
- `SelfTest.FailedRuleCount`
- `SelfTest.ValidationSummary`
- `SelfTest.Ok`
- `SelfTest.Error`
- `${OutputPrefix}.${field}`

**Особенности:** при `FailOnError = true` ошибка ставит `context.HasCriticalError = true`.
Для стандартных DUT URL (`deviceinfo`/`test.shtml`) старые короткие значения `TimeoutMs` до
`30000` автоматически расширяются до `160000`, чтобы сохранить поведение старого стенда,
который ждал загрузку selftest до 160 секунд.
Завершение временного дерева Chrome/Edge вынесено с UI-потока и ограничено отдельным
двухсекундным ожиданием, поэтому зависшее завершение браузера не блокирует окно приложения.
Извлечение понимает HTML-, URL- и JS-экранированный XML, чтобы читать данные,
которые видны в DOM/DevTools, но не отображаются в интерфейсе LuCI.
Если в XML есть дублирующиеся leaf-поля, значение верхнего уровня `<selftest>`/`<settings>`
имеет приоритет над одноименным полем из вложенного блока.
Файл `selftest.txt` больше не создается: это был legacy-артефакт старой консольной
утилиты, а актуальное приложение хранит selftest только в контексте выполнения.

**Важно:** `FailOnError` не меняет `StepResult`. При любой ошибке selftest-нода возвращает `False`; флаг нужен для итогового `test_result` в `Build Test Report`.

## Check Variable Equality

| Поле | Значение |
|---|---|
| UI title | `Check Variable Equality` |
| JSON type | `Check Variable Equality` |
| ViewModel | `CheckVariableEqualityNodeViewModel` |
| Step | `CheckVariableEqualityStep` |
| Палитра | `Проверки` |

**Коннекторы:** вход `In`, выходы `True`, `False`.

**Параметры:**

| Параметр | Дефолт |
|---|---|
| `VariableName` | `Dut.init_ok` |
| `ExpectedValue` | `1` |
| `ComparisonType` | `Number` |
| `FailMessage` | пусто |

**ComparisonType:**

- `Number`;
- `String`;
- `Boolean`;
- `HexString`;
- `Version`;
- `MacAddress`.

**Поведение:** читает переменную из `context.Variables`, нормализует по типу сравнения, сравнивает с литералом `ExpectedValue`, возвращает `True`/`False`.

**Контекст:** записывает `LastCheck.VariableName`, `LastCheck.ActualValue`, `LastCheck.ExpectedValue`, `LastCheck.Passed`.

## Check Variable Range

| Поле | Значение |
|---|---|
| UI title | `Check Variable Range` |
| JSON type | `Check Variable Range` |
| ViewModel | `CheckVariableRangeNodeViewModel` |
| Step | `CheckVariableRangeStep` |
| Палитра | `Проверки` |

**Коннекторы:** вход `In`, выходы `True`, `False`.

**Параметры:**

| Параметр | Дефолт |
|---|---|
| `VariableName` | `Dut.akb_voltage` |
| `Min` | `12.0` |
| `Max` | `27.0` |
| `Inclusive` | `true` |
| `FailMessage` | пусто |

**Поведение:** читает переменную, парсит как число, проверяет диапазон. При `Inclusive = true` границы включены, иначе используются строгие `>` и `<`.

**Контекст:** обновляет `LastCheck.*`.

## Clear ARP Cache

| Поле | Значение |
|---|---|
| UI title | `Clear ARP Cache` |
| JSON type | `Clear ARP Cache` |
| ViewModel | `ClearArpCacheNodeViewModel` |
| Step | `ClearArpCacheStep` |
| Палитра | `HTTP и сеть` |

**Коннекторы:** вход `In`, выходы `True`, `False`.

**Параметры:**

| Параметр | Дефолт |
|---|---|
| `RunArpdBat` | `true` |
| `ArpdBatPath` | `arpd.bat` |
| `Command` | `arp` |
| `Arguments` | `-d *` |
| `TimeoutMs` | `5000` |
| `FailOnError` | `false` |

**Поведение:** опционально запускает `arpd.bat`, затем запускает `Command Arguments`. Код выхода `0` считается успехом. Относительный `ArpdBatPath` сначала ищется как указан, затем рядом с exe (`AppContext.BaseDirectory`). Файл `TestBuilder/arpd.bat` копируется в output и выполняет `arp -d *`. Для совместимости старый аргумент `-d` у команды `arp` нормализуется в `-d *`.

**Контекст:** `ArpClear.ExitCode`, `ArpClear.StdOut`, `ArpClear.StdErr`, `ArpClear.Success`.

**Особенности:** если `FailOnError = false`, ошибка процесса не ломает ветку и шаг возвращает `True`.

## Configure Network Adapters

| Поле | Значение |
|---|---|
| UI title | `Configure Network Adapters` |
| JSON type | `Configure Network Adapters` |
| ViewModel | `ConfigureNetworkAdaptersNodeViewModel` |
| Step | `ConfigureNetworkAdaptersStep` |
| Палитра | `HTTP и сеть` |

**Коннекторы:** вход `In`, выходы `True`, `False`.

**Параметры:**

| Параметр | Дефолт |
|---|---|
| `AdaptersText` | `Auto=Switch;192.168.10.1;24` |
| `OutputVariableName` | `NetworkConfig` |
| `FailOnError` | `true` |

**AdaptersText:** по одной карте в строке в формате `селектор;IPv4;префикс`. Селектор — имя адаптера (`Name=Ethernet 0`), MAC (`MAC=001122334455`) или `Auto=Switch`. MAC предпочтителен, потому что имя Windows может измениться. `Auto=Switch` выбирает по порядку Ethernet-карты без IPv4-шлюза по умолчанию. Поддерживается только IPv4 с префиксом от `/1` до `/30`.

**Поведение:** только в Windows и только с правами администратора. Нода находит каждую карту, запускает `netsh` для назначения постоянного статического IPv4-адреса без gateway, затем фиксирует результат для каждой строки. Карта с IPv4-шлюзом по умолчанию защищена и никогда не изменяется — это предотвращает потерю интернет-подключения. Ошибка любой карты направляется в `False`, если включен `FailOnError`.

**Контекст:** `${OutputVariableName}.Passed`, `${OutputVariableName}.Error`, а также `${OutputVariableName}.AdapterN.Selector`, `.Address`, `.AdapterName`, `.MacAddress`, `.Success`, `.ExitCode`, `.StdOut`, `.StdErr`.

## Get Serial Number

| Поле | Значение |
|---|---|
| UI title | `Get Serial Number` |
| JSON type | `Get Serial Number` |
| ViewModel | `GetSerialNumberFromServerNodeViewModel` |
| Step | `GetSerialNumberFromServerStep` |
| Палитра | `HTTP и сеть` |

**Коннекторы:** вход `In`, выходы `True`, `False`.

**Параметры:**

| Параметр | Дефолт |
|---|---|
| `ServerBaseUrl` | пусто |
| `DeviceType` | `PSW+UPS-Box 8x2Pro` |
| `CpuIdVariableName` | `Dut.cpu_id` |
| `TimeoutMs` | `30000` |
| `RetryCount` | `1` |
| `RetryDelayMs` | `1000` |
| `OutputVariableName` | `SerialNumber` |
| `FailOnError` | `true` |

**URL:** если `ServerBaseUrl` уже заканчивается на `/getSerialNum`, используется как endpoint. Иначе строится `/api/api.svc/getSerialNum?devType=...&cpuId=...`.
Если в профиле указано `SERVER_BASE_URL` или `http://SERVER_BASE_URL`, адрес берется
из общей настройки `Server base URL`.

**Поведение:** делает HTTP GET, ожидает положительное целое число, сохраняет серийник.

**Контекст:** `OutputVariableName`, `SerialNumber`, `NetTest.SerialNumber`, `SerialNumberText`, `SerialNumberReceived`, `SerialNumberRawResponse`, `SerialNumberRequestUrl`, `SerialNumberError`.

## Send UDP Set MAC

| Поле | Значение |
|---|---|
| UI title | `Send UDP Set MAC` |
| JSON type | `Send UDP Set MAC` |
| ViewModel | `SendUdpSetMacPacketNodeViewModel` |
| Step | `SendUdpSetMacPacketStep` |
| Палитра | `HTTP и сеть` |

**Коннекторы:** вход `In`, выходы `True`, `False`.

**Параметры:**

| Параметр | Дефолт |
|---|---|
| `TargetIp` | `192.168.0.1` |
| `TargetPort` | `43962` |
| `MacVariableName` | `Dut.NewMac` |
| `TimeoutMs` | `1000` |
| `RepeatCount` | `1` |
| `DelayBetweenRepeatsMs` | `200` |
| `FailOnSendError` | `true` |

**Поведение:** читает MAC из переменной, нормализует, строит legacy-пакет длиной 21 байт и отправляет UDP на target.

**Формат пакета:** ASCII `CONFIG` в начале, `mw` с offset 10, 6 байт MAC с offset 12, `Kr2` с offset 18.

**Контекст:** `SetMac.PacketSent`, `SetMac.TargetIp`, `SetMac.TargetPort`, `SetMac.Mac`, `SetMac.PacketHex`, `SetMac.Error`.

## Run Data Test

| Поле | Значение |
|---|---|
| UI title | `Run Data Test` |
| JSON type | `Run Data Test` |
| ViewModel | `RunDataTestNodeViewModel` |
| Step | `RunDataTestStep` |
| Палитра | `HTTP и сеть` |

**Коннекторы:** вход `In`, выходы `True`, `False`.

**Параметры:**

| Параметр | Дефолт |
|---|---|
| `Mode` | `SoftwarePcap` |
| `ExpectedPackets` | `10000` |
| `PacketSizeBytes` | `1514` |
| `UdpPort` | `43962` |
| `MaxPortTestTimeMs` | `15000` |
| `TargetBandwidthMbps` | `100` |
| `DurationMs` | `5000` |
| `WarmupMs` | `500` |
| `AllowedLossPercent` | `1.0` |
| `PortsText` | `Port 0,192.168.10.1,192.168.10.2` |
| `OutputVariableName` | `DataTest` |
| `FailOnError` | `true` |

**PortsText:** одна строка на пару портов:

```text
Name,InIp,OutIp
Name,InIp,OutIp,TargetBandwidthMbps
```

**Поведение:**

- поддерживает режимы `SoftwarePcap`, `Pcap`, `Software`, `TYPE_SOFT_GEN`;
- получает список pcap-устройств через SharpPcap;
- ищет сетевые адаптеры по `InIp` и `OutIp`;
- строит Ethernet/IP/UDP packet заданного размера;
- делает warmup;
- отправляет пакеты с pacing под целевую скорость;
- считает принятые пакеты через capture filter `udp port ... and ip dst host ...`;
- проверяет loss percent и tx deficit percent против `AllowedLossPercent`.

**Контекст:**

- `${OutputVariableName}.Mode`
- `${OutputVariableName}.ExpectedPackets`
- `${OutputVariableName}.PacketSizeBytes`
- `${OutputVariableName}.UdpPort`
- `${OutputVariableName}.TargetBandwidthMbps`
- `${OutputVariableName}.DurationMs`
- `${OutputVariableName}.WarmupMs`
- `${OutputVariableName}.AllowedLossPercent`
- `${OutputVariableName}.Passed`
- `${OutputVariableName}.Error`
- `${OutputVariableName}.PortN.*`

**Особенности:** требует Npcap/WinPcap и доступ к адаптерам.

## Get UPS Status

| Поле | Значение |
|---|---|
| UI title | `Get UPS Status` |
| JSON type | `Get UPS Status` |
| ViewModel | `GetUpsStatusNodeViewModel` |
| Step | `GetUpsStatusStep` |
| Палитра | `HTTP и сеть` |

**Коннекторы:** вход `In`, выходы `True`, `False`.

**Параметры:** `BaseUrl = http://192.168.0.1`, `TimeoutMs = 5000`, `OutputVariableName = Dut.ups_rez`, `FailOnError = true`.

**Поведение:** GET `${BaseUrl}/api/getUpsStatus`, парсит целое число.

**Контекст:** output variable, `GetUpsStatus.RawResponse`, `GetUpsStatus.Success`, `GetUpsStatus.Error`.

## Get UPS Voltage

| Поле | Значение |
|---|---|
| UI title | `Get UPS Voltage` |
| JSON type | `Get UPS Voltage` |
| ViewModel | `GetUpsVoltageNodeViewModel` |
| Step | `GetUpsVoltageStep` |
| Палитра | `HTTP и сеть` |

**Коннекторы:** вход `In`, выходы `True`, `False`.

**Параметры:** `BaseUrl = http://192.168.0.1`, `TimeoutMs = 5000`, `OutputVariableName = Dut.akb_voltage`, `FailOnError = true`.

**Поведение:** GET `${BaseUrl}/api/getUpsVoltage`, парсит `double` в invariant culture.

**Контекст:** output variable, `GetUpsVoltage.RawResponse`, `GetUpsVoltage.Success`, `GetUpsVoltage.Error`.

## Get IRP Status

| Поле | Значение |
|---|---|
| UI title | `Get IRP Status` |
| JSON type | `Get IRP Status` |
| ViewModel | `GetIrpStatusNodeViewModel` |
| Step | `GetIrpStatusStep` |
| Палитра | `HTTP и сеть` |

**Коннекторы:** вход `In`, выходы `True`, `False`.

**Параметры:** `BaseUrl = http://192.168.0.1`, `TimeoutMs = 5000`, `OutputVariableName = Dut.ups_det`, `FailOnError = true`.

**Поведение:** GET `${BaseUrl}/api/isUps`, парсит целое число.

**Контекст:** output variable, `GetIrpStatus.RawResponse`, `GetIrpStatus.Success`, `GetIrpStatus.Error`.

## Build MAC From Serial

| Поле | Значение |
|---|---|
| UI title | `Build MAC From Serial` |
| JSON type | `Build MAC From Serial` |
| ViewModel | `BuildMacFromSerialNodeViewModel` |
| Step | `BuildMacFromSerialStep` |
| Палитра | `Оператор и отчеты` |

**Коннекторы:** вход `In`, выходы `True`, `False`.

**Параметры:**

| Параметр | Дефолт |
|---|---|
| `SerialVariableName` | `SerialNumber` |
| `SerialOffset` | `3200000` |
| `MacPrefix` | `C0:11:A6:20` |
| `SerialShortVariableName` | `SerialShort` |
| `MacVariableName` | `Dut.NewMac` |
| `FailOnError` | `true` |

**Поведение:** читает серийный номер, вычисляет `serialShort = serial - offset`, проверяет диапазон `0..65535`, нормализует 4-байтовый prefix MAC, формирует MAC `prefix:high:low`.

**Контекст:** `SerialShortVariableName`, `MacVariableName`, `BuildMac.SerialNumber`, `BuildMac.SerialOffset`, `BuildMac.SerialShort`, `BuildMac.Mac`, `BuildMac.Success`, `BuildMac.Error`.

## Compare Variables

| Поле | Значение |
|---|---|
| UI title | `Compare Variables` |
| JSON type | `Compare Variables` |
| ViewModel | `CompareVariablesNodeViewModel` |
| Step | `CompareVariablesStep` |
| Палитра | `Оператор и отчеты` |

**Коннекторы:** вход `In`, выходы `True`, `False`.

**Параметры:** `LeftVariableName = Dut.default_mac`, `RightVariableName = Dut.NewMac`, `ComparisonType = MacAddress`, `FailMessage = ""`.

**Поведение:** читает две переменные и сравнивает их по тем же типам, что `Check Variable Equality`.

**Контекст:** обновляет `LastCheck.*`, где actual - левая переменная, expected - правая.

## Wait Variable Until

| Поле | Значение |
|---|---|
| UI title | `Wait Variable Until` |
| JSON type | `Wait Variable Until` |
| ViewModel | `WaitVariableUntilNodeViewModel` |
| Step | `WaitVariableUntilStep` |
| Палитра | `HTTP и сеть` |

**Коннекторы:** вход `In`, выходы `True`, `False`.

**Параметры:**

| Параметр | Дефолт |
|---|---|
| `VariableName` | `Dut.ups_rez` |
| `ExpectedValue` | `1` |
| `ComparisonType` | `Number` |
| `PollAction` | `GetUpsStatus` |
| `BaseUrl` | `http://192.168.0.1` |
| `RequestTimeoutMs` | `5000` |
| `TimeoutMs` | `160000` |
| `IntervalMs` | `5000` |
| `FailOnTimeout` | `true` |

**PollAction:**

| Значение | Endpoint | Тип ответа |
|---|---|---|
| `None` | нет запроса | только проверка уже существующей переменной |
| `GetUpsStatus` | `/api/getUpsStatus` | int |
| `GetUpsVoltage` | `/api/getUpsVoltage` | double |
| `GetIrpStatus` | `/api/isUps` | int |

**Поведение:** до timeout выполняет poll action, обновляет переменную, сравнивает с expected, ждет `IntervalMs` между попытками.

**Контекст:** `WaitVariable.*` и `LastCheck.*`.

**Особенности:** поддерживает только `Number`, `String`, `Boolean`; другие типы сравнения fallback-ятся к строковому сравнению.

## Build Test Report

| Поле | Значение |
|---|---|
| UI title | `Build Test Report` |
| JSON type | `Build Test Report` |
| ViewModel | `BuildTestReportNodeViewModel` |
| Step | `BuildTestReportStep` |
| Палитра | `Оператор и отчеты` |

**Коннекторы:** вход `In`, выходы `True`, `False`.

**Параметры:**

| Параметр | Дефолт |
|---|---|
| `ReportVariableName` | `TestReportJson` |
| `DeviceName` | `PSW+UPS-Box 8x2Pro` |
| `DeviceType` | `32` |
| `SerialVariableName` | `SerialShort` |
| `MacVariableName` | `Dut.NewMac` |
| `IncludeAllVariables` | `true` |

**Поведение:** собирает JSON-отчет и кладет его в переменную `ReportVariableName`.

**Поля отчета:** `test_result`, `profile`, `device_name`, `device_type`, `serial_num`, `mac`, `created_at`, `variables`.

**Особенности:** `test_result = 1`, если `context.HasCriticalError == false`, иначе `0`.

**Результат:** при штатной работе всегда `True`. Выход `False` есть у ViewModel, но текущий `BuildTestReportStep` сам его не возвращает.

## Print Label

| Поле | Значение |
|---|---|
| UI title | `Print Label` |
| JSON type | `Print Label` |
| ViewModel | `PrintLabelNodeViewModel` |
| Step | `PrintLabelStep` |
| Палитра | `Оператор и отчеты` |

**Коннекторы:** вход `In`, выходы `True`, `False`.

**Параметры:** `PrinterName = Zebra`, `DeviceName`, `DeviceType = 32`, `SerialVariableName = SerialShort`, `MacVariableName = Dut.NewMac`, `Copies = 4`, `IncludeMac = true`, `EquipmentFieldUse = false`, `EquipmentType = 0`, `EquipmentText = ""`, `FailOnPrinterError = true`.

**Поведение:** строит ZPL, повторяет его `Copies` раз и отправляет RAW-байты в Windows spooler.

**Контекст:** `PrintLabel.Copies`, `PrintLabel.PrinterName`, `PrintLabel.Zpl`, `PrintLabel.Success`, `PrintLabel.ErrorCode`, `PrintLabel.Error`.

**Особенности:** RAW-печать доступна только на Windows. На других ОС шаг завершится ошибкой.

## Send Test Report

| Поле | Значение |
|---|---|
| UI title | `Send Test Report` |
| JSON type | `Send Test Report` |
| ViewModel | `SendTestReportNodeViewModel` |
| Step | `SendTestReportStep` |
| Палитра | `Оператор и отчеты` |

**Коннекторы:** вход `In`, выходы `True`, `False`.

**Параметры:**

| Параметр | Дефолт |
|---|---|
| `ServerBaseUrl` | `http://server-address` |
| `ReportVariableName` | `TestReportJson` |
| `Endpoint` | `/api/Api.svc/result.json` |
| `TimeoutMs` | `10000` |
| `RetryCount` | `1` |
| `RetryDelayMs` | `1000` |
| `SaveLocalCopy` | `true` |
| `LocalReportsDirectory` | `reports` |
| `FailOnError` | `false` |

**Поведение:** читает JSON отчета из переменной, опционально сохраняет локальную копию, отправляет multipart form-data POST с файлом `result.json`. Успехом считается HTTP 2xx и ответ, начинающийся с `Ok`.

**Контекст:** `SendReport.Success`, `SendReport.RawResponse`, `SendReport.Attempts`, `SendReport.Url`, `SendReport.LocalPath`, `SendReport.Error`.

## Действие оператора

| Поле | Значение |
|---|---|
| UI title | `Действие оператора` |
| JSON type | `Operator Action` |
| ViewModel | `OperatorActionNodeViewModel` |
| Step | `OperatorActionStep` |
| Палитра | `Оператор и отчеты` |

**Коннекторы:** вход `Вход`, выходы `Продолжить`, `Отмена`.

**Параметры:**

| Параметр | Дефолт |
|---|---|
| `Message` | пусто |

**Поведение:** вызывает `context.OperatorPrompt(message)`. В `TestViewModel` это открывает `OperatorActionDialog`.

**Результат:** `Продолжить` возвращает `True`, `Отмена` возвращает `False`. Если обработчик prompt не задан, шаг возвращает `False`.
